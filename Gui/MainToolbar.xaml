<UserControl x:Class="SunbirdMB.Gui.MainToolbar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:gui="clr-namespace:SunbirdMB.Gui"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SunbirdMB;component/Gui/ResourceDictionaries/Templates/LoggerDataGrid.xaml"/>
                <ResourceDictionary Source="/SunbirdMB;component/Gui/ResourceDictionaries/Templates/DarkVerticalScrollBar.xaml"/>
                <ResourceDictionary Source="/SunbirdMB;component/Gui/ResourceDictionaries/Templates/DarkTabControl.xaml"/>
                <ResourceDictionary Source="/SunbirdMB;component/Gui/ResourceDictionaries/Templates/DarkTextBox.xaml"/>
                <ResourceDictionary Source="/SunbirdMB;component/Gui/ResourceDictionaries/Templates/CubeDesignerButtonItem.xaml"/>
                <ResourceDictionary Source="/SunbirdMB;component/Gui/ResourceDictionaries/Colors.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <gui:SelectedStyleConverter x:Key="SelectedStyleConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <TabControl Background="{StaticResource VSDark2Brush}" 
                                    BorderThickness="0">
        <TabItem Header="Tools"  Foreground="{StaticResource VSTextBrush}" Width="78">
            <StackPanel Width="Auto" HorizontalAlignment="left" Orientation="Horizontal" Margin="1">
                <Button Width="Auto" Height="Auto" Command="{Binding C_WorldButtonClick}">
                    <Button.Content>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Image Source="/SunbirdMB;component/Images/WorldIcon.png" SnapsToDevicePixels="True" Width="48" Height="48"/>
                            <Label Grid.Row="1" Content="World" Foreground="WhiteSmoke" Padding="1" HorizontalAlignment="Center"/>
                        </Grid>
                    </Button.Content>
                    <Button.Style>
                        <MultiBinding Converter="{StaticResource SelectedStyleConverter}">
                            <Binding Path="IsBuilding"/>
                            <Binding Source="{StaticResource CubeDesignerSelectedItemStyle}" />
                            <Binding Source="{StaticResource CubeDesignerItemStyle}" />
                        </MultiBinding>
                    </Button.Style>
                </Button>
                <Button Width="Auto" Height="Auto" Command="{Binding C_BuildButtonClick}" Margin="2,0,0,0" >
                    <Button.Content>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Image Source="/SunbirdMB;component/Images/BuildIcon.png" SnapsToDevicePixels="True" Width="48" Height="48"/>
                            <Label Grid.Row="1" Content="Build" Foreground="WhiteSmoke" Padding="1" HorizontalAlignment="Center"/>
                        </Grid>
                    </Button.Content>
                    <Button.Style>
                        <MultiBinding Converter="{StaticResource SelectedStyleConverter}">
                            <Binding Path="IsBuilding"/>
                            <Binding Source="{StaticResource CubeDesignerItemStyle}" />
                            <Binding Source="{StaticResource CubeDesignerSelectedItemStyle}" />
                        </MultiBinding>
                    </Button.Style>
                </Button>
            </StackPanel>
        </TabItem>
    </TabControl>
</UserControl>
